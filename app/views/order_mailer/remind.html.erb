<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
</head>
<body>
<h2><%= t('mailer.title_for_order') %></h2>
<p>
  <strong><%= t('mailer.thanks_for_order') %>: </strong><br/>
</p>
<table>
  <thead>
  <tr>
    <th>
      <%= t('name') %>
    </th>
    <th>
      <%= t('price') %>
    </th>
    <th>
      <%= t('quantity') %>
    </th>
  </tr>
  </thead>
  <tbody>
  <% @order.order_products.each_with_index do |order_product, index| %>
    <% translate = order_product.product.product_translates.where(locale_id: @locale_id).first %>
    <tr>
      <td>
        <%= image_tag "#{@domain}#{order_product.product.image.url(:thumb)}", class: 'img-responsive' %>
      </td>
      <td>
        <h5>
          <%= translate.name %>
        </h5>
      </td>
      <td>
        <%= number_to_currency(translate.price) %>
      </td>
      <td>
        <%= order_product.quantity %>
      </td>
    </tr>
  <% end %>
  <% @order.order_custom_items.each_with_index do |order_custom_item, index| %>
    <tr>
      <td>
        <%= image_tag "#{@domain}#{(order_custom_item.product)? order_custom_item.product.image.url(:thumb) : order_custom_item.image.url(:thumb)}", class: 'img-responsive' %>
      </td>
      <td>
        <h5>
          <%= t('original_bag') %>
        </h5>
      </td>
      <td>
        <%= number_to_currency(order_custom_item.price) %>
      </td>
      <td>
        1
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
<h3><%= t('subtotal') %> <%= number_to_currency(@order.subtotal) %></h3>
</body>
</html>
