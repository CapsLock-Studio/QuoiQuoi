<%= render 'layouts/breadcrumbs' %>
<div class="container main-container">
  <div class="row">
    <h3 class="text-center">
      <i class="fa fa-cog fa-spin fa-fw"></i>交易進行中請稍候&nbsp;...
    </h3>
  </div>
</div>

<form action="<%= AllPay.service_url %>" method="post" class="auto-send-form">
  <% @form_data.each do |field, value| %>
  <%= hidden_field_tag field, value %>
  <% end %>

  <%= hidden_field_tag 'CheckMacValue', AllPay::Helper.check_mac_value(@form_data) %>
</form>