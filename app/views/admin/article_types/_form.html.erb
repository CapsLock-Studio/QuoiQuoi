<div class="box">
  <div class="box-header">
    <div class="title">
      <i class="icon-anchor"></i>
      文章種類
    </div>
    <div class="actions">
      <a href="#" class="btn box-collapse btn-xs btn-link"><i></i></a>
    </div>
  </div>
  <div class="box-content box-padding">
    <%= form_for([:admin, @article_type], html: { class: 'form validate-form', multipart: true }) do |f| %>
      <fieldset>
        <div class="row">
          <div class="col-sm-12">
            <div class="row">
              <%= f.fields_for :article_type_translates do |builder| %>
                <div class="col-sm-6">
                  <div class="box">
                    <div class="box-header">
                      <div class="title">
                        <%= @article_type.article_type_translates[builder.index].locale.name %>
                        <small style="margin-left:10px;">
                          <span class="label label-danger">不可留空</span>
                        </small>
                      </div>
                      <div class="actions">
                        <a class="btn box-collapse btn-xs btn-link" href="#"><i></i></a>
                      </div>
                    </div>
                    <div class="box-content">
                      <div class="form-group">
                        <%= builder.label :name, '名稱' %>
                        <div class="controls">
                          <%= builder.text_field :name, class: 'form-control',
                                                 placeholder: '請輸入文章種類名稱', data: {'rule-required' => true} %>
                        </div>
                      </div>
                      <%= builder.hidden_field :locale_id %>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </div>
        <div class="row form-actions">
          <div class="col-sm-12">
            <div class="pull-right">
              <%= link_to '取消', admin_article_types_path, class: 'btn btn-lg' %>
              <%= f.button(class: 'btn btn-primary btn-lg') do %>
                <i class="icon-save"></i>
                儲存
              <% end %>
            </div>
          </div>
        </div>
      </fieldset>
    <% end %>
  </div>
</div>