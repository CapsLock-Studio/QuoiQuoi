<li class="row" data-sub-id="<%= @course_option.id %>">
  <%= form_for [:admin, @course_option], html: {class: 'edit-sub-record', data: {'sub-target-id' => @course_option.id}} do |course_option_form| %>
    <div class="col-xs-5">
      <%= course_option_form.text_field :content, class: 'form-control', placeholder: '項目名稱' %>
    </div>
    <div class="col-xs-4">
      <%= course_option_form.text_field :price, class: 'form-control', placeholder: '價格' %>
    </div>
    <div class="col-xs-3 text-right">
      <%= link_to admin_course_option_path(@course_option),
                  class: 'btn sub-cancel-trigger',
                  data: {'sub-target-id' => @course_option.id},
                  style: 'color:#999999' do %>
        取消
      <% end %>
      <button type="submit" class="btn">
        <i class="icon-ok"></i>
      </button>
      <%= course_option_form.hidden_field :course_option_group_id %>
    </div>
  <% end %>
</li>