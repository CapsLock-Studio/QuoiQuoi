<% if flash[:message] %>
  <div class="row">
    <div class="col-xs-12">
      <h4 class="alert alert-<%= flash[:status] %> fade in">
        <button type="button" class="close" data-dismiss="alert">×</button>
        <i class="fa <%= flash[:icon] %> fa-fw"></i><%= flash[:message] %>
      </h4>
    </div>
  </div>
<% end %>