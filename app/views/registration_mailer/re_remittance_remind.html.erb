<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
</head>
<body>
<h2><%= t('mailer.title_for_re_remittance') %></h2>
<p>
  <strong><%= t('mailer.re_remittance', remittance_link: (link_to t('order.advice'), edit_payment_url(@registration.payment))).html_safe %> </strong><br/>
</p>
<h3><%= t('remitted_information') %></h3>
<table>
  <tr>
    <td>
      <%= t('remitted_amount') %>
    </td>
    <td>
      <%= number_to_currency(@registration.payment.amount) %>&nbsp;<small><%= @registration.currency %></small>
    </td>
  </tr>
  <tr>
    <td>
      <%= t('your_account') %>
    </td>
    <td>
      <%= @registration.payment.identifier %>
    </td>
  </tr>
  <tr>
    <td>
      <%= t('remitted_date') %>
    </td>
    <td>
      <%= @registration.payment.pay_time.strftime('%Y/%m/%d') %>
    </td>
  </tr>
</table>
<h3><%= t('registration.information') %></h3>
<table class="table table-hover">
  <thead>
  <tr>
    <td></td>
    <td><%= t('call_name') %></td>
    <td><%= t('tuition') %></td>
    <td><%= t('course.number') %></td>
  </tr>
  </thead>
  <tbody>
  <tr>
    <td>
      <%= image_tag "#{@domain}#{@registration.course.image.url(:thumb)}" %>
    </td>
    <td>
      <h5>
        <%= @registration.course.course_translates.where(locale_id: @registration.locale_id).first.name %>
      </h5>
    </td>
    <td>
      <%= number_to_currency(@registration.course.course_translates.where(locale_id: @registration.locale_id).first.price) %>&nbsp;<small><%= @registration.currency %></small>
    </td>
    <td>
      <%= @registration.attendance %>
    </td>
  </tr>
  </tbody>
</table>
<h3><%= t('total_amount') %>: <%= number_to_currency(@registration.subtotal) %>&nbsp;<small><%= @registration.currency %></small></h3>
<br>
<br>
<a href="http://quoiquoi.tw">quoiquoi 布知道</a>
</body>
</html>
