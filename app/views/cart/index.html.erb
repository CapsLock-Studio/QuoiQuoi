<%= render 'layouts/breadcrumbs', title: t('header.navigation.cart') %>
<div class="container">
  <div class="row">
    <div class="col-md-8">
      <div class="row">
        <% if @order.order_products.length > 0 %>
          <div class="col-md-12">
            <div class="box">
              <div class="box-header">
                <div class="title">
                  <div class="icon-list"></div>
                  <%= t('products') %>
                </div>
                <div class="actions">
                  <a class="btn box-collapse btn-xs btn-link" href="#"><i></i></a>
                </div>
              </div>
              <div class="box-content">
                <% @order.order_products.each do |order_product| %>
                  <%= render 'order_product', order_product: order_product %>
                  <hr/>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
        <% if @order.order_custom_items.length > 0 %>
          <div class="col-md-12">
            <div class="box">
              <div class="box-header">
                <div class="title">
                  <div class="icon-pencil"></div>
                  量身訂製手工包
                </div>
                <div class="actions">
                  <a class="btn box-collapse btn-xs btn-link" href="#"><i></i></a>
                </div>
              </div>
              <div class="box-content">
                <% @order.order_custom_items.each do |order_custom_item| %>
                  <%= render 'order_custom_item', order_custom_item: order_custom_item %>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="col-md-4">
      <div class="box">
        <div class="box-header">
          <div class="title">
            <div class="glyphicon-saved glyphicon"></div>
            小計
          </div>
          <div class="actions">
            <a class="btn box-collapse btn-xs btn-link" href="#"><i></i></a>
          </div>
        </div>
        <div class="box-content">
          <h4 class="margin-bottom-30">
            <strong>
              <span style="color:#666;">總金額</span>
              <span class="pull-right">
                <%= number_to_currency(@subtotal) %>
              </span>
            </strong>
          </h4>
          <div class="row">
            <div class="col-md-12 margin-bottom-10">
              <%= link_to '結算', new_order_path, class: 'btn btn-block btn-u-orange btn-lg hover-effect', style: 'color:#FFF;' %>
            </div>
            <div class="col-md-12">
              <%= link_to products_path do %>
                <i class="icon-angle-left"></i>
                繼續逛逛
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>